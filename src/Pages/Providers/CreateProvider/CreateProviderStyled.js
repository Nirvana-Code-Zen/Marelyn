import styled from 'styled-components'

const CreateProviderStyled = styled.div`
`

export const BtnCreateProvider = styled.div`
  position: absolute;
  bottom: 9rem;
`
export default CreateProviderStyled
